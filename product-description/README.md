The `product-description` partial should be called like this:

```hbs
{{> productDescription ProductDescription=Certificate.ProductDescription CertificateLanguages=Certificate.CertificateLanguages}}
```
